from django.apps import AppConfig


class RagDirectoryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "rag_directory"
    verbose_name = "RAG & Vector DB Directory"
